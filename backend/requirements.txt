# Smart Mid-Day Meal Management System - API dependencies
#
# These are the exact versions the application is verified to run on.
# Requires Python 3.12 or newer.
#
#   python -m venv venv
#   venv/Scripts/activate        (Windows)  |  source venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt
#
# The one pairing to be careful with is passlib 1.7.4 + bcrypt 3.x: passlib
# reads bcrypt.__about__, which bcrypt 4 removed, so bcrypt>=4 breaks every
# password check with a confusing traceback. Keep bcrypt on 3.x.

# --- Web framework ---------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# --- Database --------------------------------------------------------------
SQLAlchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.6

# --- Authentication & security ---------------------------------------------
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==3.2.2

# --- Configuration & validation --------------------------------------------
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
python-dotenv==1.2.2

# --- Face recognition & computer vision ------------------------------------
# insightface brings in numpy, onnx, scikit-image, scipy and tqdm itself; the
# pins below fix the versions that matter for the attendance pipeline.
insightface==1.0.1
onnxruntime==1.28.0
opencv-python==5.0.0.93
numpy==2.5.1

# --- AWS S3 (optional: photo storage falls back to local disk without it) ---
boto3==1.43.98

# --- Utilities -------------------------------------------------------------
loguru==0.7.3
Pillow==12.3.0
